org: patdevng
app: expense-tracker-1
service: expense-tracker-1
frameworkVersion: '3'

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs14.x

functions:
  endpoint:
    handler: handler.endpoint
    events:
      - httpApi:
          path: /pong
          method: get
